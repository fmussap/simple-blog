# Simple blog app using ReactJs + Router + Redux

Simple blog app using **ReactJs**, **Router**, and **Redux**.

To install it:
1) Clone this repo;
2) In the main directory run ``yarn``

that's it !!
